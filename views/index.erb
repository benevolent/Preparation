<h1>DisplayなPage</h1>
<p>
  <% if @list.empty? %>
    データを入れろ
  <% else %>
    <% @item = @list.sample%>
    今回の開発は <a href="<%= h @item.title %>"><%= h @item.title %></a> をつくりましょう
  <% end %>
 </p>
